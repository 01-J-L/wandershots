# Copy this to .env and fill in values
FLASK_DEBUG=1
SECRET_KEY=change_me_please
# PostgreSQL DSN via psycopg3 (recommended)
# DATABASE_URL=postgresql+psycopg://flask_user:strong_password@localhost:5432/flask_demo
# If DATABASE_URL is not set, the app will fallback to SQLite (dev.db)
